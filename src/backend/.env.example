# MongoDB Connection String (Get from MongoDB Atlas)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/registration

# Port (Render assigns automatically, optional for local dev)
PORT=5500
